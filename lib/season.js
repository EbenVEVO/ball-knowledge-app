// Real "current" season (2026) has no ingested data yet - every card reads this
// instead of seasons.current=true. Flip back to a dynamic lookup once data exists.
export const DISPLAY_SEASON = 2026
